# Stable demo-video link

Submit this stable URL: `https://ch920425.github.io/nativas/demo/`.

It intentionally shows a small placeholder until the video is ready. To activate the redirect, replace the empty string in `docs/demo/config.js` with the final public video URL and push to `main`. GitHub Pages redeploys automatically; the submitted URL stays unchanged.

Use only a public, HTTPS video URL. Verify the redirect in an incognito browser before submitting.
